//*******************************************************
// ShoreSide Configuration File

ServerHost = localhost
ServerPort = 9000
Community  = shoreside

#include plug_origin_warp.moos
  
//--------------------------------------------------------
Processconfig = ANTLER
{
  MSBetweenLaunches = 100

  Run = MOOSDB          @ NewConsole = false
  Run = pRealm          @ NewConsole = false
  Run = pMarineViewer   @ NewConsole = false
  Run = pLogger         @ NewConsole = false
  Run = pShare          @ NewConsole = false
  Run = pHostInfo       @ NewConsole = false
  Run = uFldShoreBroker @ NewConsole = false
  Run = pRealm          @ NewConsole = false
  Run = uTimerScript    @ NewConsole = false
  Run = pPointAssign    @ NewConsole = false
}

#include plug_pShare.moos
#include plug_pHostInfo.moos
#include plug_pLogger.moos

//--------------------------------------------------
// uFldShoreBroker Configuration Block

ProcessConfig = uFldShoreBroker
{
  AppTick       = 1
  CommsTick     = 1

  qbridge  = DEPLOY, NODE_REPORT
  qbridge  = STATION_KEEP, APPCAST_REQ
  qbridge  = MOOS_MANUAL_OVERRIDE
  qbridge  = REFUEL_NEEDED
  qbridge  = TSP

  bridge   = src=RETURN_ALL, alias=RETURN
  bridge   = src=RETURN_$V,  alias=RETURN
  bridge   = src=REFUEL_NEEDED_ALL, alias=REFUEL_NEEDED
  bridge   = src=REFUEL_NEEDED_$V,  alias=REFUEL_NEEDED
  bridge   = src=TSP_ALL, alias=TSP
  bridge   = src=TSP_$V,  alias=TSP
  bridge   = src=LOITER_ALL, alias=LOITER
  bridge   = src=LOITER_$V,  alias=LOITER
  bridge   = src=VISIT_POINT_$V,  alias=VISIT_POINT
}

//--------------------------------------------------------
ProcessConfig = pMarineViewer
{
  AppTick    = 4
  CommsTick  = 4

  tiff_file            = forrest19.tif
  set_pan_x            = -90
  set_pan_y            = -280
  zoom                 = 0.65
  vehicles_shape_scale = 1.5
  vehicles_name_mode   = names+shortmode

	nodes_font_size = xlarge
	procs_font_size = xlarge
	appcast_font_size = large
	
  point_viewable_labels   = true
  polygon_viewable_labels = false
  appcast_viewable     = true
  appcast_color_scheme = indigo
  hash_delta           = 50
  hash_viewable        = true
  hash_shade           = 0.35

  scope = PHI_HOST_INFO

  button_one   = DEPLOY  # DEPLOY_ALL=true
  button_one   = MOOS_MANUAL_OVERRIDE_ALL=false
  button_one   = RETURN_ALL=false
  
  
  button_two   = RETURN  # RETURN_ALL=true
  button_two   = DEPLOY_ALL=true
  button_two   = MOOS_MANUAL_OVERRIDE_ALL=false
  button_two   = STATION_KEEP_ALL=false
  button_two   = TSP_ALL=false
  button_two   = LOITER_ALL=false
  button_two   = REFUEL_NEEDED_ALL=false

  button_three = STATION  # STATION_KEEP_ALL=true
  button_three = DEPLOY_ALL=true
  button_three = MOOS_MANUAL_OVERRIDE_ALL=false
  button_three = REFUEL_NEEDED_ALL=false

  button_four  = REFUEL  # STATION_KEEP_ALL = false
  button_four  = REFUEL_NEEDED_ALL=true

  button_five  = TSP  # STATION_KEEP_ALL = false
  button_five  = LOITER_ALL = false
  button_five  = TSP_ALL=true
  
  button_six   = LOITER  # STATION_KEEP_ALL = false
  button_six   = TSP_ALL=false
  button_six   = LOITER_ALL=true
  
  }
//--------------------------------------------------------
ProcessConfig = uTimerScript
{
  AppTick    = 4
  CommsTick  = 4

  reset_max  = 0
  reset_time = all-posted
  block_on   = pPointAssign

  rand_var    = varname=x_nav, min=-25, max=200, key=at_post, snap=1
  rand_var    = varname=y_nav, min=-175, max=-25, key=at_post, snap=1

  event = var=VISIT_POINT, val="firstpoint", time=1
  event = var=VISIT_POINT, val="x=$[x_nav], y=$[y_nav], id=$[TCOUNT]", time=2, amt=100
  event = var=VISIT_POINT, val="lastpoint", time=3
}

//--------------------------------------------------------
ProcessConfig = pPointAssign
{
  AppTick   = 4
  CommsTick = 4

  vname = gilda
  vname = henry
  assign_by_region = true
}
